const Header = (): React.ReactElement => {
  return (
    <div
      className={
        "flex items-center justify-center px-4 py-6 shadow-2xl bg-yellow-400"
      }
    >
      header section
    </div>
  );
};

export default Header;
